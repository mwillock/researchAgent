services:
 api:
  command: uvicorn api.app.main:app --host 0.0.0.0 --port 8000 --workers 2
  environment:
    APP_ENV: prod
    LOG_LEVEL: info
  restart: unless-stopped

  db:
    restart: unless-stopped 